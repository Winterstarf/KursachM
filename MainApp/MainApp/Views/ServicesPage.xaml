<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MainApp.Views.ServicesPage">
    <StackLayout>
        <ListView x:Name="ServicesListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="{Binding Description}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
            <Button Text="Add" Clicked="OnAddServiceClicked" />
            <Button Text="Edit" Clicked="OnEditServiceClicked" />
            <Button Text="Delete" Clicked="OnDeleteServiceClicked" />
        </StackLayout>
    </StackLayout>
</ContentPage>
